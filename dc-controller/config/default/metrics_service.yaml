apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.openshift.io/serving-cert-secret-name: dc-controller-metrics-tls
  labels:
    control-plane: manager
    app.kubernetes.io/name: dc-controller
    app.kubernetes.io/managed-by: kustomize
  name: metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: manager
    app.kubernetes.io/name: dc-controller
